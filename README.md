# Juggle

Personal juggling skill tracker.

MVP: a single static `index.html` with progress stored in browser `localStorage`.

## Run

Open `index.html` in a browser.

## Current scope

- 3-ball core patterns
- basic 4-ball and 5-ball milestones
- left/right progress for asymmetric patterns
- simple dependency tree
- repeat list
- JSON export
